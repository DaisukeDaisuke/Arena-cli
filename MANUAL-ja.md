# VOID PIT: BORROWED CROWN

地下闘技場で5連戦するコマンド戦闘ゲームです。敵の次の行動を見て、攻撃・防御・技を選んでください。

## 起動

Windows TerminalのPowerShellでexeのあるフォルダーを開き、次を実行します。

```powershell
.\void-pit-windows-x64.exe
```

表示にはUTF-8とANSIカラーに対応した端末を使ってください。通常画面は76列以上を想定しています。文字や色が崩れる環境、出力をファイルに残す場合はASCIIの絵で表示できます。

```powershell
.\void-pit-windows-x64.exe --plain
```

新しく始めるたびに対戦相手と技の組合せが変わります。入力してEnterを押すと進みます。急いで入力する必要はなく、矢印キーも使いません。

画面が変わると8個の改行を挟み、新しい画面を下へ表示します。前の画面はスクロールして読み返せます。対応端末で毎回画面を消したい場合は次を使います。

```powershell
.\void-pit-windows-x64.exe --redraw=clear
```

表示がおかしい場合はこの指定を外してください。`--plain`と`--redraw=clear`は併用できません。

## 画面の読み方

| 表示 | 意味 |
|---|---|
| HP | 体力。0になると敗退します |
| MP | 技を使う力。技ごとに必要量が異なります |
| SPD | 素早さ。高い側が先に動き、同値なら自分が先です |
| GOLD | 買物に使う所持金 |
| NEXT | 敵が次に行う行動。下の日本語説明も確認してください |
| ORDER | 行動順。YOUが自分、ENEMYが相手です |
| FOCUS | 集中。次の直接攻撃を強めます |
| BURN | 継続ダメージの残り回数 |
| BANK | 防御で蓄積した力 |

経路の水色の`HERE`は現在地、緑の`DONE`は通った場所、黄色の番号は選べる道です。`LOCK`は今は選べない場所です。色が出ない場合も文字で区別できます。

## 戦闘

`1`・`2`・`3`で画面に並ぶ装備技を使います。技名を入力する場合は名前を全部入力してください。

| 入力 | 操作 |
|---|---|
| `attack` | 通常攻撃 |
| `guard` | その回の直接攻撃を半分にし、自分の行動時にMPを4回復 |
| `wait` | 防御せず、自分の行動時にMPを6回復 |
| `next` | 画面に案内が出ているとき、自分の自動行動を進める |
| `skills` | 今装備している技と日本語の効果説明 |
| `book` | 覚えた技全部と、現在の装備枠 |
| `status` | 能力・装備・状態を確認 |
| `recent` | 最近使った戦闘コマンドを確認 |
| `? fi` | `fi`で始まる基本技・装備技の候補を表示 |
| `help` | 操作説明 |
| `back` | 情報画面から元の画面へ戻る |

情報表示、無効入力、MP不足で戦闘は進みません。閲覧中は`back`で戻ってから行動してください。補完候補からの自動実行はありません。1試合は20ラウンドまでです。

## 技の習得と買物

途中の習得所では技を1つ覚え、3つの装備枠のどこへ入れるか選びます。一度覚えた技は枠から外しても忘れません。習得所で`equip`を入力すると、覚えた技への装備変更ができます。`0`で習得所を出ます。

敵を倒すと所持金が増えます。店では防具と装飾品を買えます。番号で商品を選び、購入確認で`1`を選ぶと購入します。`2`なら戻ります。購入済みの商品も番号で選び直せます。

購入前にHP・MP・SPDの変化を表示します。体力が増えても遅くなる装備があります。次の相手の技と素早さを見て選んでください。買わずに所持金を残しても構いません。

装備は`0`で店を出るときに確定します。店内での試着では実際のHP・MPは変わりません。購入した品の売却・返品はできません。

## 終了と中断再開

`quit`を入力し、確認画面で`1`を選ぶと終了します。`2`または空行で元の画面に戻ります。

入力が途切れた場合は敗退にせず、カレントフォルダーへ中断記録を保存します。画面に表示されたファイル名を指定して再開できます。

```powershell
.\void-pit-windows-x64.exe --resume arena-suspend-0123456789abcdef-12.txt
```

ファイル名は例です。実際に表示された名前へ置き換えてください。未確認の購入や未確定の習得選択は保存されず、直前の確定した操作まで戻ります。

「保存失敗」と表示された場合は再開ファイルが正常にできていません。画面のseedと操作列を残してください。中断記録は同じ版のexeで使用してください。
